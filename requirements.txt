# ============================================================
# Water transparency retrieval in Lake Titicaca
# Python dependencies for pipeline/ (p00-p12) and figures/
#
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   bash pipeline/run_all.sh
#
# Versions are those the reported results were produced with.
# Figures are built in R, not Python: see the R package list in
# the README.
# ============================================================

# ---- Data processing --------------------------------------
numpy==2.5.3
pandas==3.0.5
scipy==1.18.1

# ---- Models -----------------------------------------------
scikit-learn==1.9.0     # Random Forest, ExtraTrees, GroupKFold, linear
xgboost==3.4.1          # p06, model-family comparison only
lightgbm==4.7.0         # p06, model-family comparison only
optuna==5.0.0           # p06, hyperparameter search

# ---- Interpretability -------------------------------------
shap==0.52.0            # p08, out-of-fold TreeExplainer

# ---- Utilities --------------------------------------------
joblib==1.6.0           # model persistence

# ---- Spatial ----------------------------------------------
geopandas==1.1.4        # p12 (Moran/LISA), basemap, graphical abstract
shapely==2.1.2
pyproj==3.8.0
pyogrio==0.13.0
rasterio==1.5.1         # graphical abstract (SRTM relief)

# ---- Figures and Earth Engine -----------------------------
matplotlib==3.11.1      # graphical abstract (3D)
pillow==12.3.0          # fig02 zoom-line measurement
earthengine-api==1.7.42 # p11 and basemap extraction only; needs credentials
