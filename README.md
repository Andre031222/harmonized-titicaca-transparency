# Harmonization and validation limits of satellite Secchi retrieval in Lake Titicaca

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Python 3.11](https://img.shields.io/badge/python-3.11-blue.svg)](https://www.python.org/)
[![R 4.4+](https://img.shields.io/badge/figures-R%20%2F%20ggplot2-276DC3.svg)](https://www.r-project.org/)
[![Journal](https://img.shields.io/badge/target-Journal%20of%20Great%20Lakes%20Research-1a7f8e.svg)](https://www.sciencedirect.com/journal/journal-of-great-lakes-research)
[![Status](https://img.shields.io/badge/status-preparing%20submission-orange.svg)](#citation)

Reproducible analysis for **Secchi disk depth (*Z*<sub>SD</sub>) retrieval** in
**Lake Titicaca** (3,810 m a.s.l.) from **Sentinel-2 MSI and Landsat 8/9 OLI**
surface reflectance, validated against twelve years of IMARPE limnological
monitoring.

> **Companion code for the manuscript:**
> *Harmonization and validation limits of satellite Secchi retrieval in
> Lake Titicaca.*
> Being prepared for submission to the *Journal of Great Lakes Research*
> (Elsevier / IAGLR). **Not yet under review.**

The paper is as much about **how to evaluate** a machine-learning water-quality
product as about the product itself. Two defaults imported from the turbid-water
literature fail in clear, campaign-sampled water, and we give a cheap diagnostic
for each.

---

## Table of contents
- [Key results](#key-results)
- [Repository structure](#repository-structure)
- [Data](#data)
- [Installation](#installation)
- [Reproducing the study](#reproducing-the-study)
- [Methodology](#methodology)
- [Figures](#figures)
- [Honest scope and limitations](#honest-scope-and-limitations)
- [Citation](#citation)
- [License & contact](#license--contact)

---

## Key results

Every number below is regenerated by `pipeline/run_all.sh`. Values in the
manuscript's running text are macros written by `p09` into
`manuscript/jglr/numbers.tex`; values in its tables are typed, and `p10`
checks all 117 of them against `results/tidy/` on every run, failing the
build on any mismatch. **It found two stale cells the first time it ran.**

### 1. Land-derived harmonization is invalid over dark water

The per-band coefficients of Roy et al. (2016), fitted on vegetation and soil,
are routinely reused to merge Landsat into Sentinel-2 archives. Over clear
water their additive intercepts **exceed the entire native Landsat signal**:

| Band | Blue | Green | Red | NIR | SWIR1 | SWIR2 |
|---|:--:|:--:|:--:|:--:|:--:|:--:|
| Intercept / native signal | 2.5× | 1.4× | 10.6× | **69.8×** | 23.2× | 8.2× |

A Random Forest recovers the **source sensor** from the *already harmonized*
reflectance with **99.8 % accuracy** (chance 66.3 %) — the retrieval was
learning a sensor label, not unified optics. Across 270 same-day paired
observations, agreement is acceptable in green (*r* = 0.71) but collapses in
blue (*r* = 0.26), the band the retrieval most depends on.

**Diagnostic we propose:** a classifier two-sample test. If a classifier can
tell the sensors apart after harmonization, the harmonization has not worked.

### 2. The campaign, not the station, is the blocking unit

Blocking cross-validation by monitoring station is the usual remedy for spatial
autocorrelation. In a campaign-based programme it leaves the dominant
dependence untouched. Three null models — each predicting **only the
training-set mean of one grouping** — evaluated under each blocking design:

| Null model ↓ / folds blocked by → | Station | Campaign date | Year (= whole campaign) |
|---|:--:|:--:|:--:|
| Zone mean | 0.276 | 0.287 | 0.234 |
| Station mean | −0.008 | 0.208 | 0.222 |
| Campaign-date mean | **0.305** | −0.012 | −0.032 |

Each unit collapses its own null by construction (the diagonal). What matters
is what survives off it: under **station-blocked** folds a null that knows only
the campaign date still explains **R² = 0.305** — the temporal structure passes
through station blocking untouched. The zone mean explains 0.23–0.29 under
*every* design: that is the trophic gradient, a property of the lake a model
should learn, not leakage. Consistently, station blocking changes the result
not at all:

| Validation design | R² | RMSE (m) | n |
|---|:--:|:--:|:--:|
| Random K-fold | 0.596 | 1.81 | 812 |
| Station-blocked | 0.596 | 1.81 | 812 |
| **Campaign-date-blocked (primary)** | **0.574** | **1.86** | 812 |
| Year-blocked (= whole campaign) | 0.495 | 2.02 | 812 |
| One record per in-situ event | 0.571 | 1.77 | 540 |

The 9 field campaigns span 7–27 days each (median 13 sampling dates), so
date-blocking can still put two days of one campaign on opposite sides of the
split. Year-blocking isolates whole campaigns (one per year) and gives 0.495;
the 0.079 gap bounds the residual within-campaign dependence. Date-blocking
stays primary because nine year-groups leave too few folds for stable
cross-validation and conformal calibration; both figures are reported.

### 3. Retrieval, benchmarks and where it stops working

Random Forest, campaign-blocked, **R² = 0.574, RMSE 1.86 m, MAE 1.44 m,
bias −0.02 m (n = 812)**:

| Model | R² | RMSE (m) |
|---|:--:|:--:|
| **Random Forest (this study)** | **0.574** | 1.86 |
| Multiband linear regression | 0.323 | 2.34 |
| Classical blue/green ratio, bounded (Kloiber 2002) | 0.097 | 2.71 |
| Classical blue/green ratio, unbounded as published | −2.458 | 5.29 |

The unbounded baseline is a straw man: undoing the log without bounds emits a
single **143.5 m** prediction (real maximum 16.5 m) that alone drives R² to
−2.46. Baselines are only informative when implemented correctly.

**Extrapolation fails.** Withholding an entire trophic zone:

| Held-out zone | R² | RMSE (m) | bias (m) |
|---|:--:|:--:|:--:|
| Lago Menor | 0.247 | 1.90 | +0.02 |
| Bahía de Puno | 0.036 | 2.11 | +1.35 |
| Lago Mayor | −0.047 | 2.52 | −1.48 |

The model interpolates within sampled optical regimes; it does not transfer to
an unseen water type.

### 4. Uncertainty is calibrated on average, not conditionally

Split-conformal prediction intervals:

| Nominal | 50 % | 80 % | 90 % | 95 % |
|---|:--:|:--:|:--:|:--:|
| Empirical coverage | 50.9 % | 79.1 % | **89.4 %** | 94.3 % |
| Mean width (m) | 2.50 | 4.61 | 6.12 | 7.37 |

Marginally well calibrated — but a single global quantile cannot adapt, so
coverage falls to **71.3 % in the clearest water** and 79.5 % in the most
turbid. A marginal coverage figure alone would hide this.

### 5. Only transparency is retrievable in this water

Same honest design applied to every co-measured variable:

| Variable | R² | n | Retrievable |
|---|:--:|:--:|:--:|
| Transparency (Secchi) | **0.574** | 812 | ✅ |
| Water temperature (in-situ) | 0.113 | 1,002 | ❌ |
| Suspended solids | 0.056 | 920 | ❌ |
| Temperature (thermal ST_B10) | 0.020 | 662 | ❌ |
| Chlorophyll-*a* | 0.012 | 1,002 | ❌ |

Their in-situ ranges lie below the optical/thermal detection limit in this
oligotrophic regime. The negative claim is held to the same standard as the
positive one.

---

## Repository structure

```
.
├── pipeline/                        Analysis, in execution order
│   ├── p00_config.py                Paths, features, seed, shared validation design
│   ├── p01_build_dataset.py         Analysis dataset + real inventory (missing years/months)
│   ├── p02_fix1_harmonization.py    Roy (2016) harmonization over dark water
│   ├── p03_fix2_baselines.py        Correctly implemented classical baseline
│   ├── p04_fix3_validation.py       Blocking unit and null models
│   ├── p05_fix4_temporal.py         Temporal hold-out, seasonality, trend fragility
│   ├── p06_model_selection.py       Nested CV + Optuna (slow, ~1 h)
│   ├── p07_uncertainty.py           Conformal intervals, marginal and conditional coverage
│   ├── p08_interpretability.py      Out-of-fold SHAP + retrievability of other variables
│   ├── p09_export_manuscript_numbers.py   Writes numbers.tex
│   ├── p10_verify_manuscript.py     Checks the manuscript against results/tidy/
│   └── run_all.sh                   Reproduces everything, end to end
├── figures/R/                       ggplot2 figure scripts (fig01–fig08) + shared theme
├── data/
│   ├── processed/                   Match-up tables (matchups_s2.csv, matchups_ls.csv)
│   ├── insitu/reference/            OEFA 2016 reference values + source metadata
│   └── lake_boundary/               Lake geometry (Natural Earth, GPKG)
├── results/
│   ├── metrics/                     Metrics as JSON (one file per pipeline stage)
│   ├── tidy/                        Every table behind a figure, as CSV
│   └── figures_r/                   Manuscript figures (PNG 300 dpi + vector PDF)
├── manuscript/jglr/                 LaTeX source (elsarticle), numbers.tex, compiled PDF
└── requirements.txt
```

---

## Data

| Source | What | Availability |
|---|---|---|
| **IMARPE** limnological campaigns | In-situ Secchi, chlorophyll-*a*, nutrients, etc. — 881 measurements, 2011–2024, 160 station identifiers (154 distinct stations) | Published records; the July 2019 campaign is in Siguayro & Franco (2022). The raw compilation is not redistributed here; available on request. |
| **OEFA (2016)** | Reference values, inner Puno Bay | Public government report |
| **Sentinel-2 / Landsat 8-9** | Surface reflectance | Free via [Google Earth Engine](https://earthengine.google.com) |
| **Match-up tables** (`data/processed/`) | 1,002 satellite ↔ in-situ pairs (340 Sentinel-2, 662 Landsat 8/9); 812 carry a Secchi reading | **Included in this repository** |

The analysis runs from the match-up tables onward, so everything in the
manuscript is reproducible from this repository alone, without Earth Engine
credentials. The Earth Engine extraction that produced those tables is in the
git history (`scripts/_legacy/10_build_matchups.py`,
`12_build_matchups_landsat.py`, superseded by `pipeline/`).

> **Provenance check:** our 2019 zone-level means reproduce the published IMARPE
> values almost exactly (Lago Mayor conductivity 1513.4 vs 1513.5 µS/cm;
> Lago Menor chlorophyll-*a* 1.52 vs 1.52 mg/m³). **No synthetic or
> spectrally-derived labels are used at any stage.**

---

## Installation

```bash
git clone https://github.com/Andre031222/harmonized-titicaca-transparency.git
cd harmonized-titicaca-transparency

python3.11 -m venv .venv          # or: uv venv --python 3.11
.venv/bin/pip install -r requirements.txt
```

`run_all.sh` expects the interpreter at `.venv/bin/python`.

**Figures** additionally need R (≥ 4.4) with `ggplot2`, `patchwork`, `dplyr`,
`tidyr`, `readr`, `forcats`, `scales`, `stringr`, `sf` and `viridis`:

```r
install.packages(c("ggplot2","patchwork","dplyr","tidyr","readr",
                   "forcats","scales","stringr","sf","viridis"))
```

Compiling the PDF needs a LaTeX distribution with `elsarticle`.

---

## Reproducing the study

```bash
bash pipeline/run_all.sh            # everything: metrics, figures, PDF (~1.5 h)
bash pipeline/run_all.sh --fast     # skip p06 model selection (the slow stage)
bash pipeline/run_all.sh --no-tex   # skip the LaTeX build
```

Stages can also be run individually, in the order listed in
[Repository structure](#repository-structure) — note `p03` imports from `p04`,
so it runs after it. Figures are rebuilt with:

```bash
cd figures/R && for f in fig0*.R; do Rscript "$f"; done
```

Outputs land in `results/metrics/` (JSON), `results/tidy/` (CSV) and
`results/figures_r/` (PNG + PDF). `p09` then writes
`manuscript/jglr/numbers.tex`, which the manuscript reads, and `p10` verifies
the typed table cells against the CSVs before the PDF is built — so a re-run
cannot leave a stale number anywhere in the article.

```bash
python pipeline/p10_verify_manuscript.py   # on its own; exit 1 on any mismatch
```

---

## Methodology

1. **Match-ups** — for each in-situ measurement, cloud-masked median reflectance
   in a 30 m buffer at the true station coordinate, within ±10 days.
2. **Harmonization** — Landsat SR mapped to Sentinel-2-equivalent reflectance
   with Roy et al. (2016); six common bands (B2, B3, B4, NIR, B11, B12). The
   paper shows this transform is invalid over this water and quantifies by how
   much.
3. **Features** — twelve sensor-agnostic features: the six bands plus NDWI,
   NDTI and the blue/green, green/blue, red/green and blue/red ratios.
4. **Model** — Random Forest; scaler fitted **inside the training fold only**.
   XGBoost, ExtraTrees, LightGBM and a multiband linear model are reported for
   comparison; nested CV with Optuna confirms tuning does not move the ceiling.
5. **Validation** — the blocking unit is *diagnosed*, not assumed: the three
   null models are run under **every** candidate blocking design. Each unit
   collapses its own null by construction, so the diagnosis is what survives
   off that diagonal — and the campaign-date null survives station blocking
   at R² = 0.305. Leave-one-zone-out then bounds extrapolation.
6. **Uncertainty** — split-conformal prediction intervals, reported both
   marginally and conditionally on measured transparency.
7. **Interpretation** — out-of-fold SHAP, so each observation is explained by a
   model that never saw it.

---

## Figures

All figures are in [`results/figures_r/`](results/figures_r/), as 300 dpi PNG
and vector PDF, at Elsevier column widths (90 / 140 / 190 mm):

| File | Content |
|---|---|
| `fig01_harmonization_failure` | Why land-derived harmonization fails over dark water |
| `fig02_study_area` | Monitoring network and sampling effort |
| `fig03_validation_design` | Null models per blocking unit; blocking vs extrapolation |
| `fig04_retrieval_and_benchmark` | Predicted vs measured, residuals by zone, benchmarks |
| `fig05_data_inventory` | What the dataset actually contains (gaps, seasonality, granularity) |
| `fig06_trends_and_start_year` | In-situ trends and their sensitivity to the start year |
| `fig07_conformal_uncertainty` | Conformal coverage, marginal and conditional |
| `fig08_shap_and_retrievability` | Out-of-fold SHAP and the retrievability envelope |

---

## Honest scope and limitations

- **Transparency is retrievable** (R² = 0.574, RMSE 1.86 m under the strict
  design); **chlorophyll-*a*, suspended solids and temperature are not.**
- **The result is an interpolation, not a transfer.** Skill collapses when an
  entire trophic zone is withheld.
- **Dry-season only.** Every match-up falls in July–October; there are none in
  the wet season, when blooms occur in Bahía de Puno.
- **Three years have no campaign at all** (2020, 2021, 2023), so a hold-out
  described as "2022–2024" contains only 2022 and 2024.
- **Trends are fragile.** No zone shows a significant Mann–Kendall trend over
  the full record, but starting the series in 2013 instead of 2011 turns two
  zones "significant". We report the full record and the sensitivity table.
- **The reference measurement is coarse.** Secchi readings are rounded to about
  half a metre, so an RMSE of 1.86 m is within a small multiple of the
  granularity of the reference itself.

---

## Citation

The manuscript is not yet published. If you use this code or the match-up
tables, please cite the repository and check back for the article reference:

```bibtex
@misc{titicaca_transparency_code,
  author = {Yana-Yucra, Dina Maribel and Vilca-Solorzano, Richar Andre
            and Torres-Cruz, Fred and Iba{\~n}ez, Vladimiro},
  title  = {Harmonization and validation limits of satellite {Secchi}
            retrieval in {Lake Titicaca}: analysis code and match-up data},
  year   = {2026},
  howpublished = {\url{https://github.com/Andre031222/harmonized-titicaca-transparency}}
}
```

---

## License & contact

Released under the [MIT License](LICENSE).

**Corresponding author:** Richar Andre Vilca-Solorzano — `75521963@est.unap.edu.pe`
Faculty of Statistical and Computer Engineering, Universidad Nacional del
Altiplano (UNAP), Puno, Peru.

*In-situ data courtesy of the IMARPE and OEFA monitoring programmes. Satellite
imagery via Google Earth Engine.*
